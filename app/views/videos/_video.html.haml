.video{class: "#{cycle('firstInRow', '', '')}"}
  .videoThumbnail
    = link_to image_tag(video.thumbnail_url, width: 306, height: 172), video.player_url, class: 'fancybox', 'data-width' => video.width, 'data-height' => video.height, 'data-title' => video.title
  .videoTitle
    = video.title